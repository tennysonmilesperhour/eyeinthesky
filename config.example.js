// config.example.js — copy to config.js and fill in before loading
// the extension. config.js is gitignored.

const CONFIG = {
  INGEST_URL: 'https://geck-data.vercel.app/api/ingest',
  STATS_URL: 'https://geck-data.vercel.app/api/stats',
  API_KEY: 'REPLACE_ME_WITH_STRONG_RANDOM_TOKEN',
  SUSPICIOUS_PRICE: 10000,
  VERSION: '3.0.0'
};
